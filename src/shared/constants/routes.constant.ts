// NOTE: these constants below must be matched with the pages folder structure

export const APP_ROUTES = {
  // Public routes
  INTRODUCTION: '/intro',

  // Private routes
  HOME: '/home',
};
